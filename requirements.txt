fastapi>=0.110
uvicorn[standard]>=0.27
requests>=2.31
networkx>=3.1
numpy>=1.24
# Local DEM sampling (Copernicus GLO-30 / SRTM tiles). Required in Docker; for
# local dev without GDAL hassles you can omit it and the app uses the elevation
# API fallback automatically.
rasterio>=1.3
